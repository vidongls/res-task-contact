import React from 'react'
import { Card, Layout } from 'antd'

interface Props {}

const ContactDetailProductDetail = (props: Props) => {
  const { Content } = Layout
  return (
    <Content className="details-info-content">
      <Card></Card>
    </Content>
  )
}

export default ContactDetailProductDetail
